name: Test Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
  #    - name: Install NodeJs
  #      uses: actions/npm@5
  #      with:
  #        node-version: 18
      - name: Install dependency
        run: npm ci
      - name: Run tests 
        run: npm test  
         
